# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/knowledge-builder

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development

# Firebase Admin SDK Service Account Key (JSON string)
# Get this from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Convert the JSON to a single-line string and paste here
FIREBASE_SERVICE_ACCOUNT_KEY={\
type\:\service_account\,\project_id\:\your-project-id\,...}

# Gemini API (for AI summaries)
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-1.5-flash
